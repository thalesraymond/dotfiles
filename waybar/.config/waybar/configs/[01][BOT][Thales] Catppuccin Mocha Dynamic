{
  "include": [
    "$HOME/.config/waybar/Modules",
    "$HOME/.config/waybar/ModulesCustom",
    "$HOME/.config/waybar/ModulesGroups",
    "$HOME/.config/waybar/UserModules"
  ],
  "layer": "top",
  "position": "bottom",
  "height": 30,
  "output": "DP-3",
  "margin-bottom": 5,
  "margin-left": 300,
  "margin-right": 300,
  "modules-left": ["hyprland/workspaces#rwCustom", "hyprland/window"],
  "modules-center": ["clock"],
  "modules-right": [
    "pulseaudio",
    "cpu",
    "memory",
    "disk",
    "custom/updater",
    "tray"
  ],
  "hyprland/window": {
    "format": "{initialTitle}"
  },
  "clock": {
    "format": "{:%H:%M:%S %d/%m/%Y}"
  },
  "pulseaudio": {
    "format": "   {volume}%",
    "format-muted": " muted"
  },
  "cpu": {
    "format": "  {usage}%", // CPU icon + usage %
    "max-decimals": 0
  },
  "memory": {
    "format": "󰾆  {percentage}%"
  },
  "disk": {
    "format": "  {percentage_used}%"
  }
}
